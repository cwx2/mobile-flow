# App Icons

Place platform-specific icons here:

- `icon.ico` — Windows (256x256, multi-resolution)
- `icon.icns` — macOS
- `icon.png` — Linux (256x256)

The tray module generates a simple programmatic icon as fallback
when these files are not present.
